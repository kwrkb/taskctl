# Build artifacts (YAML→JSON 変換の生成物)
src/Taskctl/data/*.json

# .NET (v2 / Taskctl.Cli)
bin/
obj/

# Publish output (NativeAOT single exe) と Release アセット
build/publish/
build/*.zip

# Pester / test output
testResults.xml
coverage.xml

# Editor / OS
.vscode/
*.swp
Thumbs.db
